# Copy this file to .env.local and fill in your local values.
# .env.local overrides .env for local development only.

NODE_ENV=development
PORT=5000

# Remote Hostinger MySQL — enable Remote MySQL in hPanel first, then get the host
DATABASE_URL="mysql://u856082912_cebusafetour:8gcQDX8%3D8Dr@YOUR_HOSTINGER_MYSQL_HOST:3306/u856082912_cebusafetourdb"

# OR use a local MySQL instance:
# DATABASE_URL="mysql://root:@localhost:3306/cebusafetour_dev"

CORS_ORIGINS=http://localhost:5173,http://localhost:3000
