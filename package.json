{
  "name": "cebusafetour",
  "version": "1.0.0",
  "description": "CebuSafeTour monorepo — Express + React admin",
  "main": "backend/src/app.js",
  "scripts": {
    "build": "npm install --prefix backend --include=dev && npm install --prefix admin && npm run build --prefix admin",
    "start": "node backend/src/app.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
